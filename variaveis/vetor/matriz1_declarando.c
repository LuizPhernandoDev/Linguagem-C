#include <stdio.h>

//pode se ter n linha e colunas ,ex: matriz[i][j][k][l]
int main() {
	int tabela[3][4];   // Matriz de números inteiros com 3 linhas e 4 colunas
	float notas[5][2];  // Matriz com 5 linhas (ex: alunos) e 2 colunas (ex: provas)
    
    return 0;
}